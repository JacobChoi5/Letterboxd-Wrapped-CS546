<main>
    <h2>{{Title}}</h2>
    <ol>
            {{#each accounts}}
            <li>
                <a href="/{{_id}}">{{username}}</a>
            </li>
            {{/each}}
        </ol>
</main>